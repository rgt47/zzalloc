% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/unrestricted.R
\name{alloc_random_allocation}
\alias{alloc_random_allocation}
\title{Random allocation to a fixed arm size}
\usage{
alloc_random_allocation(n)
}
\arguments{
\item{n}{Number of subjects. Odd \code{n} gives arms differing by one.}
}
\value{
An integer vector of length \code{n}.
}
\description{
A permutation of exactly \code{n / 2} assignments to each arm, so
final imbalance is zero by construction. Unlike simple
randomization the assignments are not independent, and the last
subjects in the sequence are highly predictable once the earlier
ones are known.
}
\examples{
set.seed(1)
table(alloc_random_allocation(20))
}
