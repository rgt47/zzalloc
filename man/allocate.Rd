% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/allocate.R
\name{allocate}
\alias{allocate}
\title{Allocate by a named scheme}
\usage{
allocate(scheme, n = NULL, covariates = NULL, ...)
}
\arguments{
\item{scheme}{One of the values returned by
\code{\link[=allocation_schemes]{allocation_schemes()}}.}

\item{n}{Number of subjects. Required for schemes that do not
take covariates; ignored, with a warning if inconsistent, for
those that do.}

\item{covariates}{Data frame of balancing factors, required for
the covariate-adaptive and stratified schemes.}

\item{...}{Passed to the underlying function.}
}
\value{
An integer vector of assignments. For \code{"cara"} a list,
as documented in \code{\link[=alloc_cara]{alloc_cara()}}.
}
\description{
A single entry point to every procedure in the package, so that
a simulation can iterate over schemes without a switch
statement at each call site. Arguments specific to a scheme are
passed through \code{...}.
}
\examples{
set.seed(1)
allocate("simple", n = 10)
cv <- data.frame(sex = sample(c("F", "M"), 20, TRUE))
allocate("pocock_simon", covariates = cv, p = 0.9)
}
