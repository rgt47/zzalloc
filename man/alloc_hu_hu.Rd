% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/minimization.R
\name{alloc_hu_hu}
\alias{alloc_hu_hu}
\title{Hu-Hu joint balance minimization}
\usage{
alloc_hu_hu(
  covariates,
  p = 0.8,
  weights = NULL,
  overall_weight = 1,
  stratum_weight = 1,
  measure = c("sd", "diff", "squared")
)
}
\arguments{
\item{covariates}{A data frame of discrete balancing factors.}

\item{p}{Probability of assignment to the arm minimizing the
weighted score. Default 0.8.}

\item{weights}{Optional per-covariate weights for the marginal
terms, named or positional.}

\item{overall_weight}{Weight on imbalance in the total sample
size. Default 1.}

\item{stratum_weight}{Weight on imbalance within the subject's
joint stratum. Default 1. Setting this to 0 and
\code{overall_weight} to 0 recovers \code{\link[=alloc_pocock_simon]{alloc_pocock_simon()}}.}

\item{measure}{Imbalance measure. See \code{\link[=alloc_pocock_simon]{alloc_pocock_simon()}}.}
}
\value{
An integer vector with one assignment per row.
}
\description{
Extends Pocock-Simon by scoring three kinds of imbalance at
once: overall imbalance in the total sample size, marginal
imbalance within each factor level, and imbalance within the
subject's own joint stratum. The stratum term is what
distinguishes it, since a design balancing only margins can be
badly unbalanced within cells, which matters when the outcome
depends on a covariate interaction.
}
\details{
Hu and Hu establish that the family attains bounded imbalance
on all three scales simultaneously under conditions on the
weights, which is not true of marginal balancing alone.
}
\examples{
set.seed(1)
cv <- data.frame(sex = sample(c("F", "M"), 40, TRUE),
                 stage = sample(c("I", "II"), 40, TRUE))
alloc_hu_hu(cv, stratum_weight = 2)
}
\references{
Hu Y, Hu F (2012). Asymptotic properties of
covariate-adaptive randomization. \emph{Annals of Statistics}
40(3):1794-1815.
}
