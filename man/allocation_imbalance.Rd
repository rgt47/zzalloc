% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/imbalance.R
\name{allocation_imbalance}
\alias{allocation_imbalance}
\title{Imbalance diagnostics for a realized allocation}
\usage{
allocation_imbalance(trt, covariates = NULL)
}
\arguments{
\item{trt}{Integer vector of assignments, \code{0} or \code{1}.}

\item{covariates}{Optional data frame of balancing factors, one
row per assignment.}
}
\value{
A list with \code{overall}, the difference in arm sizes;
\code{marginal}, a named list of per-level differences for each
covariate; \code{max_marginal}, the largest absolute marginal
difference; and \code{stratum}, per-cell differences for the joint
cross-classification. The covariate entries are \code{NULL} when
no covariates are supplied.
}
\description{
Reports imbalance on the three scales the procedures in this
package target, so that a design can be judged on the scale it
was built for rather than on whichever is convenient.
}
\examples{
set.seed(1)
cv <- data.frame(sex = sample(c("F", "M"), 40, TRUE))
a <- alloc_pocock_simon(cv)
allocation_imbalance(a, cv)
}
